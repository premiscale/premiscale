version: "3.9"
services:
  state:
    image: docker-develop.ops.premiscale.com/mysql-doppler:latest
    build: ./docker/mysql
    depends_on: []
    privileged: false
    container_name: mysql
    ports:
      - 3306:3306
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.5"
          memory: 500M
      restart_policy:
        condition: on-failure
        delay: 1s
        max_attempts: 3
        window: 240s
    environment:
      - DOPPLER_TOKEN

  metrics:
    image: docker-develop.ops.premiscale.com/influxdb-doppler:latest
    build: ./docker/influxdb
    depends_on: []
    privileged: false
    container_name: influxdb
    ports:
      - 8086:8086
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.5"
          memory: 500M
      restart_policy:
        condition: on-failure
        delay: 1s
        max_attempts: 3
        window: 240s
    environment:
      - DOPPLER_TOKEN

  premiscale:
    image: docker-develop.ops.premiscale.com/premiscale:latest
    build: ./docker/premiscale
    depends_on:
      - metrics
      - state
    privileged: true
    container_name: premiscale
    ports: []
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "1.0"
          memory: 2G
        reservations:
          cpus: "0.5"
          memory: 1G
      restart_policy:
        condition: on-failure
        delay: 1s
        max_attempts: 3
        window: 240s
    environment:
      - DOPPLER_TOKEN
